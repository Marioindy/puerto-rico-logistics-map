version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            # Use install instead of ci so added deps (like zod) sync the lockfile automatically in Amplify builds
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**
