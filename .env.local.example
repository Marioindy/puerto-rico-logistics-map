# Copy to .env.local for local development. These values are read at build/runtime.

# Public client-side variables (bundled to browser)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Server-side variables (never exposed to browser)
CONVEX_DEPLOYMENT=
AMPLIFY_ENV=
PPLX= # Perplexity API key used by server route /api/chat

#This Example is only for local development. For production, use your own values on AWS Amplify and Convex.