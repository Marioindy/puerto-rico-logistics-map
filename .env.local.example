# AWS Amplify Production Deployment Configuration
#
# This application is designed for AWS Amplify hosting only.
# All secrets are managed through AWS Amplify Console: Hosting → Secrets
#
# Configured secrets (set in Amplify Console):
# - PPLX (Perplexity API key for chat functionality)
# - CONVEX_DEPLOYMENT (Convex deployment ID - reserved)
# - CONVEX_URL (Convex URL - reserved)
# - GOOGLE_MAPS_API_KEY (Google Maps API key)
#
# These secrets are automatically injected as environment variables
# during the build process via amplify.yml configuration.
#
# No local development setup required - deploy directly to AWS Amplify.